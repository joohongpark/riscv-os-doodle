# 1000줄로 OS 만들기

[링크](https://operating-system-in-1000-lines.vercel.app/ko/) 를 보고 내 환경에 맞게 세팅하고, 따라한 이력입니다.

## 1. 환경설정
Mac OS 환경이지만, homebrew를 사용하지 않으므로 llvm과 QEMU를 직접 다운로드 / 빌드함.

### 1-1. LLVM - 공식 pre-built 바이너리 사용
[GitHub Release](https://github.com/llvm/llvm-project/releases) 에서 빌드된 결과물을 다운로드
 - 나는 `clang+llvm-18.1.8-arm64-apple-macos11.tar.xz` 사용

```shell
# 압축 해제
tar xf clang+llvm-18.1.8-arm64-apple-macos11.tar.xz

# 폴더명 변경
mv clang+llvm-18.1.8-arm64-apple-darwin22.0 llvm

# PATH 설정
export PATH="${$(pwd)}/llvm/bin:$PATH"
```

### 1-2. QEMU - 직접 빌드 필요

```shell
# QEMU 소스 다운로드
curl -LO https://download.qemu.org/qemu-8.2.0.tar.xz
tar xf qemu-8.2.0.tar.xz
cd qemu-8.2.0

# 최소 기능만으로 빌드
./configure \
    --prefix=$HOME/qemu \
    --target-list=riscv32-softmmu \
    --disable-gtk \
    --disable-sdl \
    --disable-vnc \
    --disable-gnutls \
    --disable-nettle \
    --disable-gcrypt \
    --enable-cocoa \
    --without-default-features \
    --enable-tcg

make -j$(sysctl -n hw.ncpu)

# 동적 라이브러리 의존성 확인
otool -L bin/qemu-system-riscv32

# 의존성이 존재하는 라이브러리는 별도로 이동시켜야 함.

# dylib 라이브러리 경로 설정
export DYLD_LIBRARY_PATH=./qemu:${DYLD_LIBRARY_PATH:-}
```

## 2. 사용 가능한지 테스트
```shell
$ clang -print-targets | grep riscv32
# -> riscv32     - 32-bit RISC-V 가 출력되어야 함.

$ qemu-system-riscv32 -machine virt -bios default -nographic -serial mon:stdio --no-reboot
# Ctrl + A -> C 누른 후, q 눌러서 종료
```
